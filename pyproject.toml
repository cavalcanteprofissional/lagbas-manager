[project]
name = "labgas-manager"
version = "1.0.0"
requires-python = ">=3.11"

[tool.uv]
python = "3.11"

[tool.vercel]
pythonVersion = "3.11"

[[tool.vercel.builds]]
src = "frontend/app.py"
use = "@vercel/python"

[[tool.vercel.builds]]
src = "backend/app.py"
use = "@vercel/python"